function mult(primeiroNumero: number, segundoNumero: number): number {
    return primeiroNumero * segundoNumero;
}

function dizOlá(nome: string): string {
    return "Olá " + nome;
}

console.log(mult(5, 5));
console.log(dizOlá("Garbriel"));
